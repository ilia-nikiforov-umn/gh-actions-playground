name: Testing

on: [push]

jobs:
    list-pip:
        runs-on: ubuntu-22.04

        steps:
            - name: list python modules
              run: |
                python -m pip list
            - name: print interesting github variables
              env:
                ACTION_REPOSITORY: ${{ github.action_repository }}
              run: |
                printf $ACTION_REPOSITORY